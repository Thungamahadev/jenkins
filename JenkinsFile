   pipeline {
   agent any
   stages {
     stage('build') {
     steps { 
     sh 'String string = "This is a string to check whether a string contains a substring";'
     sh 'String substring = "string";'
     sh 'println(string.contains(substring));'
}
}
}
}
