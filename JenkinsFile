   pipeline {
   agent any
   stages {
     stage('build') {
     steps { 
     String string = "This is a string to check whether a string contains a substring";
     String substring = "string";
     println(string.contains(substring));
}
}
}
}
